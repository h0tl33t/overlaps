require 'spec_helper'

module Overlaps
  describe StartPoint do
    it 'has a value'
    it 'has an id it belongs to'
    
    context "#start?" do
      it 'returns true'
    end
    
    context "#end?" do
      it 'returns false'
    end
  end
end